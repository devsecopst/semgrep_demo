repos:
  - repo: https://github.com/semgrep/pre-commit
    rev: 'v1.130.0'
    hooks:
      - id: semgrep
        entry: semgrep
        args: ['--config', 'p/secrets','--config','p/comment','--skip-unknown-extensions','--error']
        verbose: true
