repos:
  - repo: local
    hooks:
      - id: semgrep
        name: semgrep
        entry: semgrep
        language: system
        args: ["--config", "p/secrets", "--config", "p/comment"]
        verbose: true
